package com.cn.drools;
import com.cn.drools.pojo.People
import com.cn.drools.pojo.Cat
dialect  "java"

rule "girl3"
    when
        People(sex == 0) and Cat(sex == 0)
    then
        System.out.println("girl3规则执行");
end
